/**
 * Application Constants
 *
 * Centralised constants for Display Lab.
 * BASE_URL is temporary — replace after DevOps confirms the real deployment URL.
 */

/** Canonical base URL (no trailing slash). Replace after deployment. */
export const BASE_URL = 'https://displaylab.vercel.app';
